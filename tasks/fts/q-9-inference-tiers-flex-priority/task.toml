schema_version = "1.3"
artifacts = [
    { source = "/app/output.txt", destination = "output.txt" }
]

[task]
name = "ivanleo/fts-q-9-inference-tiers-flex-priority"
description = "Evaluation task for q-9-inference-tiers-flex-priority"
keywords = ["Inference Tiers", "Flex Inference", "Priority Inference", "Graceful Degradation"]

[[task.authors]]
name = "Ivan Leo"
email = "ivanleomk@gmail.com"

[metadata]

[verifier]
timeout_sec = 600.0

[verifier.env]
GEMINI_API_KEY = "$GEMINI_API_KEY"

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
